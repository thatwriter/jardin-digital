<%*
    let files = app.vault.getFiles();
    let latestNoteNumber = 0;
    const folderPath = "Zettelkasten/Físico"; // Reemplaza esto con la ruta real de tu carpeta
    const filePrefix = "A"; // Reemplaza esto si tu prefijo es diferente
    
    // Filtra los archivos por la carpeta y el prefijo para encontrar la última nota
    let latestNote = files
        .filter(file => file.path.startsWith(folderPath) && file.basename.startsWith(filePrefix))
        .sort((a, b) => {
            let numA = parseInt(a.basename.replace(filePrefix, ""));
            let numB = parseInt(b.basename.replace(filePrefix, ""));
            return numB - numA;
        })[0];
    
    if (latestNote) {
        // Extrae el número de la última nota y lo convierte a un entero
        latestNoteNumber = parseInt(latestNote.basename.replace(filePrefix, ""));
    }
    
    // Incrementa el número para la nueva nota
    let newNoteNumber = latestNoteNumber + 1;
    let newTitle = `${filePrefix}${newNoteNumber}`;
    
    // Renombra la nota actual con el nuevo título
    await tp.file.rename(newTitle);
    let titleInput = await tp.system.prompt("Ingresa el título para la nota:");
    -%>---
title: "<% titleInput %>"
---
## <% titleInput %>
Contenido
Fuente: 

---
## Vínculos a otras notas
